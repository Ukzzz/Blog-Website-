<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Edit Blog</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-900 text-white min-h-screen p-6">
    <div class="max-w-2xl mx-auto bg-white text-gray-900 p-8 rounded-xl shadow-lg">
      <h1 class="text-2xl font-bold mb-6">✏️ Edit Blog Post</h1>
      <form action="/viewBlog/<%= blog._id %>?_method=PUT" method="POST" class="space-y-4">
        <div>
          <label class="block font-semibold">Title</label>
          <input type="text" name="title" value="<%= blog.title %>" required
            class="w-full p-2 border border-gray-300 rounded" />
        </div>
        <div>
          <label class="block font-semibold">Content</label>
          <textarea name="content" rows="6" required
            class="w-full p-2 border border-gray-300 rounded"><%= blog.content %></textarea>
        </div>
        <div class="flex gap-4">
          <button type="submit"
            class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 transition">Update</button>
          <a href="/viewBlog" class="text-blue-300 hover:underline">Cancel</a>
        </div>
      </form>
    </div>
  </body>
</html>
