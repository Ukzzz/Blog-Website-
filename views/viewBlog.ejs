<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Blogs</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" />
    <style>
        body {
            font-family: "Inter", sans-serif;
        }
        .truncated {
            max-height: 8rem;
            overflow: hidden;
        }
    </style>
</head>
<body class="bg-gray-950 text-gray-100 min-h-screen">
    <header class="bg-gray-900 border-b border-gray-800 shadow-lg sticky top-0 z-50">
        <div class="max-w-6xl mx-auto px-4 py-4 flex justify-between items-center">
            <h1 class="text-2xl font-bold bg-gradient-to-r from-blue-400 to-purple-500 bg-clip-text text-transparent">
                📝 My Blog
            </h1>
            <nav class="space-x-6 text-sm font-medium">
                <a href="/" class="text-gray-300 hover:text-white transition">🏠 Home</a>
                <a href="/blog" class="text-gray-300 hover:text-white transition">➕ New Post</a>
            </nav>
        </div>
    </header>
    <main class="max-w-6xl mx-auto px-4 pt-12 pb-20">
        <h2 class="text-4xl font-extrabold mb-10 text-center tracking-tight text-white">
            📚 Latest Blogs
        </h2>
        <div class="flex flex-wrap gap-8 justify-center items-start">
            <% blogs.forEach(blog => { %>
                <div class="bg-white/90 backdrop-blur-md rounded-2xl shadow-lg p-6 border border-gray-300 w-full sm:w-1/2 lg:w-1/3 transition-all duration-300 hover:scale-105 hover:shadow-xl">
                    <h3 class="text-xl font-bold text-blue-800 mb-3">
                        <%= blog.title %>
                    </h3>
                    <div class="text-sm text-gray-500 mb-3">
                        👤 <%= blog.author || "Unknown Author" %> • 🕒
                        <%= blog.createdAt ? blog.createdAt.toDateString() : 'Today' %>
                    </div>
                    <div>
                        <p id="content-<%= blog._id %>"
                           class="text-gray-700 leading-relaxed tracking-wide transition-all duration-500 ease-in-out truncated">
                            <%= blog.content %>
                        </p>
                    </div>
                    <button onclick="toggleContent('<%= blog._id %>')"
                            class="text-indigo-600 font-semibold hover:text-indigo-800 transition mt-2"
                            id="toggleBtn-<%= blog._id %>">
                        🔎 Read More
                    </button>
                    <div class="flex justify-between items-center mt-4">
                        <form action="/viewBlog/<%= blog._id %>?_method=DELETE" method="POST"
                              onsubmit="return confirm('Are you sure you want to delete this blog post?')">
                            <button type="submit" class="text-red-600 font-semibold hover:text-red-800 transition">
                                🗑 Delete
                            </button>
                        </form>
                        <a href="/edit/<%= blog._id %>"
                           class="text-blue-600 font-semibold hover:text-blue-800 transition">
                            ✏️ Edit
                        </a>
                    </div>
                </div>
            <% }) %>
        </div>
    </main>
    <footer class="text-center text-sm text-gray-500 border-t border-gray-800 pt-6 mt-16">
        © <%= new Date().getFullYear() %> My Blog Platform. All rights reserved.
    </footer>
    <script>
        function toggleContent(id) {
            const content = document.getElementById(`content-${id}`);
            const btn = document.getElementById(`toggleBtn-${id}`);
            content.classList.toggle("truncated");
            if (content.classList.contains("truncated")) {
                btn.innerText = "🔎 Read More";
            } else {
                btn.innerText = "🔼 Read Less";
            }
        }
    </script>
</body>
</html>